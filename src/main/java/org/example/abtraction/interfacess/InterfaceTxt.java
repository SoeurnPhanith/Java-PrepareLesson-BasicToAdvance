package org.example.abtraction.interfacess;

public class InterfaceTxt {
    /*


       1.What is interface?
        => interface jea pneak muy ney abstraction deal jea blue print(template) deal
        store tea abstract method pon nos dea use for hide data.

       2.interface attribute ?
        => interface having some attibute :
        1.inheritance or abstarction can't use mulitple inheritance but
          interface can use mulitple inheritance
        2.Define rule of code
        3.interface can't be having constructure in it
        4.using keyword implement to implementation data from base class to do
        5.vea mean default accessing (public abtract) & (public static final)
        6.interface kor min ja mean normal propoties but can having constrant propotie in it
        7.using keyword interface

       3.Why use interface?
        => we use interface for :
            1. create standard classs
            2. using abstraction + inheritance
            3. Dependencies Injection (uisng in spring framework)
            4. polymorphism (reference object)
    */
}
